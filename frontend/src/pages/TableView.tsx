/** Stub page — full implementation in Sub-Task 7. */

export default function TableView() {
  return (
    <div
      style={{
        padding: 32,
        fontFamily: '-apple-system, "Segoe UI", system-ui, sans-serif',
        color: "#1f2328",
      }}
    >
      <h2 style={{ margin: "0 0 8px", fontWeight: 600, fontSize: 20 }}>
        Faction Territory — Table View
      </h2>
      <p style={{ color: "#57606a", fontSize: 14 }}>
        Select a faction and (optionally) a center system to populate the table.
        Full implementation in Sub-Task 7.
      </p>
    </div>
  );
}
